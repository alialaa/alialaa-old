import { css } from "@emotion/core";

const styles = css`
    .container {
        max-width: max(60vw, 1000px);
        padding: 0 2rem;
        margin: 0 auto;
        z-index: 10;
    }
`;

export default styles;
