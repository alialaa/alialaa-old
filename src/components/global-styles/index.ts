import { css } from "@emotion/core";

const styles = css`
    :root {
        --main: #f03;
    }
    html {
        font-size: 62.5%;
    }
    body {
        font-family: "Visby", Arial, Helvetica, sans-serif;
        font-size: 1.6rem;
        font-weight: 400;
        line-height: 1.5;
        color: #000000;
        text-align: left;
    }
`;

export default styles;
