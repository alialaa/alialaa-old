import { css } from "@emotion/core";
import { mq } from "@utils";

const styles = css`
    padding: 8rem 0;
    /* background: #130f1e; */
    .copyright {
        text-align: center;
        margin-top: 12rem;
        font-weight: 600;
    }
`;

export default styles;
